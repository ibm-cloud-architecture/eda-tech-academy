apiVersion: mq.ibm.com/v1beta1
kind: QueueManager
metadata:
  name: store-mq
  namespace: std-1-rt-inventory
spec:
  license:
    accept: true
    license: L-RJON-C7QG3S
    use: NonProduction
  queueManager:
    name: QM1
    mqsc:
      - configMap:
          name: mq-mqsc-config
          items:
            -  example.mqsc
    resources:
      limits:
        cpu: 250m
      requests:
        cpu: 250m
    metrics:
      enabled: false
    route:
      enabled: true
    storage:
      queueManager:
        type: ephemeral
  template:
    pod:
      containers:
        - name: qmgr
          env:
            - name: MQSNOAUT
              value: 'yes' 
          envFrom:
          - configMapRef:
              name: mq-config

  version: 9.2.5.0-r2
  web:
    enabled: true

